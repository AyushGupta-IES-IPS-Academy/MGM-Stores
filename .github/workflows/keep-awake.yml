name: Keep Render Alive

on:
  schedule:
    - cron: "*/10 * * * *"   # Runs every 10 minutes
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Curl Render site
        run: curl -s https://mgm-stores.onrender.com/ > /dev/null
